import styles from "./reservationHero.scss";

const ReservationHero = () => {
  return (
    <header className={styles.reservationHero}>
      <div className={styles.contentWrapper}></div>
    </header>
  );
};

export default ReservationHero;
